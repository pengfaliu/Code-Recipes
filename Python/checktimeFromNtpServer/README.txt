#这里说明该程序的使用说明
#该程序目的是配合检测服务器时间和ntp时间的是否有差值
#该程序主要用于zabbix，与zabbix配合使用
#该程序的精度分两种
#1.是 低精度，精度是1秒，使用算法的粗粒度
#2.是 高精度，在 10ms < 精度 <1000ms 但程序默认检测的是100ms的精度
#3.程序要求，ntp server 与 本程序 在同一个时区。
#该程序最后更新 2015-12-12 
#建议运行版本  2.6 < python版本 <3.0 